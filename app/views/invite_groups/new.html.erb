<%= form_for(@invite_group) do |f|%>
	グループ
	<br>
	<%= f.collection_select :group_id, @groups, :id, :name %>
	<br>
	招待するユーザー
	<br>
	<%= f.collection_select :invited_user, @users, :id, :name %>
	<br>
	<%= f.submit "招待" %>
<% end %>